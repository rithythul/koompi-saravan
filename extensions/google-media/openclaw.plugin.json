{
  "id": "google-media",
  "name": "Google Media Tools",
  "description": "Nano Banana (image gen), Veo (video), Lyria (music), TTS for social media content",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "geminiApiKey": {
        "type": "string",
        "description": "Google AI Studio API key for Gemini"
      },
      "defaultOutputDir": {
        "type": "string",
        "description": "Root directory where generated assets and rendered videos are stored"
      },
      "dryRun": {
        "type": "boolean",
        "description": "Generate and render assets without publishing side effects"
      },
      "killSwitch": {
        "type": "boolean",
        "description": "Stop all automation when set to true"
      }
    }
  },
  "uiHints": {
    "geminiApiKey": {
      "label": "Gemini API Key",
      "sensitive": true
    },
    "defaultOutputDir": {
      "label": "Output Directory"
    },
    "dryRun": {
      "label": "Dry Run"
    },
    "killSwitch": {
      "label": "Kill Switch"
    }
  }
}
